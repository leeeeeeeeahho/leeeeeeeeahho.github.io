完成！解壓後的專案結構如下：
credit_risk_project/
├── app.py              ← Flask 主程式 + /api/risk 端點
├── requirements.txt    ← flask>=3.0.0
├── README.md           ← 完整使用說明
├── templates/
│   └── index.html      ← Jinja2 模板（已移除「族群」頁面）
└── static/
    ├── css/style.css   ← 所有樣式
    ├── js/main.js      ← 前端邏輯（fetch API 呼叫後端）
    └── img/hero.jpg    ← 首頁背景圖
啟動步驟：
bashpip install -r requirements.txt
python app.py
# 開啟 http://localhost:5000